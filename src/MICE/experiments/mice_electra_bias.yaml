id: train_mice_electra_l8+3_bias
title: "Try to train a mice electra with 4 and 8 contextualization layers"
description:
    Using the best parameters from the grid search,
    we run additional trainings validating on nanobeir to see the impact on OOD.

module: MICE.experiments.mice_training
parent: mice_training_base.yaml
# pre_experiment: pre_experiment

retrieval:
    requirements: duration=20h & cpu(cores=16) & cuda(mem=20G) * 2
    k: 1000
    batch_size: 64

evaluation: #default = Minified tests
    all_datasets: false
    in_domain: true
    beir13: true

base: google/electra-base-discriminator
n_contextualization_layers: 8
n_interaction_layers: 3
extra_attn_bias: true
